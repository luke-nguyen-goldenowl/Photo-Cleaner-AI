import 'package:flutter/material.dart';

class CleanerView extends StatelessWidget {
  const CleanerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Cleaner View'),
    );
  }
}
